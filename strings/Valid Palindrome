class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = ''.join(letter for letter in s if letter.isalnum()).lower()

        if s==s[::-1]:
            return True
        else:
            return False
       
